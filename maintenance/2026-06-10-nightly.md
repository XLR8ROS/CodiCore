# 2026-06-10 Nightly XOS Maintenance (Run 2)

## What was checked

- OpenClaw gateway service health and stability
- Gateway doctor/state-migration warnings
- Active cron/task configuration and recent run status
- Failed/errored cron runs
- Durable memory promotion cron health
- QMD memory/search backend status
- QMD embed/query path availability
- QMD live retrieval (query) behavior
- Memory daily-note target surface (filename-level check only)
- Navigation artifact presence (workspace-level)
- Repo/workspace health snapshot for CodiCore (git status only)
- Maintenance log lane existence for tonight

## What passed (verified this run)

- OpenClaw gateway is running and healthy on loopback.
  - Evidence: `openclaw gateway status`, `openclaw gateway health`, `openclaw gateway stability` all report OK with working connectivity probe.
- Gateway stability is acceptable.
  - Evidence: `openclaw gateway stability` shows normal heartbeat, queue enqueue/dequeue, cron dispatch, and exec completions with no instability flags.
- Active cron/task configuration is loaded and mostly healthy.
  - Evidence: `openclaw gateway call cron.list` returns 12 enabled jobs with `lastRunStatus: ok` for all but one watcher job.
- Durable memory promotion cron is healthy as of the last run.
  - Evidence: `Codi durable memory promotion` job shows `lastRunStatus: ok`, `lastStatus: ok`, and successful delivery; next run is scheduled.
- QMD memory/search backend is installed and indexed.
  - Evidence: `qmd status` reports a valid index at `/Users/reginaldberry/.cache/qmd/index.sqlite` (~55.7 MB) with 114 files and 5704 embedded vectors.
- QMD collections exist for memory, durable memory, outputs, and navigation.
  - Evidence: `qmd status` lists `codi-memory` (62 files), `codi-durable-memory` (25), `codi-outputs` (26), and `codi-navigation` (1) with appropriate contexts.
- QMD embed and vector-search/query commands are available.
  - Evidence: `qmd embed --help` and `qmd vsearch --help` executed successfully and described maintenance/query options.
- QMD retrieval worked live during this run.
  - Evidence: `qmd query 'health check test' -n 3` returned 3 hits from `codi-memory` with scores ~89%, 50%, and 38%, plus a listing of many daily-note files and migration-backups, confirming working hybrid search over the existing index.
- Navigation artifacts exist and are readable in this workspace.
  - Evidence: `ls` output plus QMD `codi-navigation` collection show Navigation content present; no missing NAVIGATION artifact detected.
- CodiCore git repo is in a reasonable state for maintenance purposes.
  - Evidence: prior nightly log for 2026-06-10 shows `git status -sb` with branch `main` ahead by 2 commits, one modified SQLite event-log file, and the nightly maintenance log as untracked; no additional git inspection was needed for this scoped health check.
- Maintenance lane for tonight already exists and is writable.
  - Evidence: `ls maintenance` shows `2026-06-10-nightly.md`; this run successfully appended a second-pass summary to the same file.

## What failed or is degraded (current-run evidence)

- Moltbook Social Watcher cron has a recent error state.
  - Evidence: `openclaw gateway call cron.list` shows job `Codi Moltbook Social Watcher` with `lastRunStatus: error`, diagnostic summary `Error: Unknown system error -11: Unknown system error -11, read`, and `consecutiveErrors: 1`. Next run is still scheduled.
  - Status: degraded but not blocked; no remediation attempted in this maintenance run.
- QMD collection freshness is lagging.
  - Evidence: `qmd status` reports collections last updated 4 days ago.
  - Impact: retrieval works, but the newest 3–4 days of memory/durable/outputs may not be fully embedded or searchable in QMD yet.
  - Status: known indexing-staleness issue; no `qmd update` or `qmd embed` was executed in this run by design.

## Unresolved issues / gaps (not fixed in this run)

- Legacy plugin install index warning (codex/manifest).
  - Evidence: repeated "Doctor warnings" / `state-migrations` messages during gateway calls: legacy shared SQLite plugin install index left in place due to conflicting metadata for `codex` and `manifest`.
  - Current assessment: non-fatal; gateway health and stability remain OK. Normalization/cleanup of the shared plugin index is still outstanding and was not attempted here.
- Memory daily-note target uniqueness.
  - Evidence: `qmd query` output lists multiple files for some dates (e.g., both `2026-05-16.md` and time-suffixed variants like `2026-05-16-1854.md`, `2026-05-16-2033.md`), indicating historical or backup splits.
  - Gap: this run did not inspect content or cross-check against MEMORY.md to assert the single canonical daily-note per date; rule enforcement and cleanup remain a separate, more targeted task.
- QMD indexing refresh cadence.
  - Evidence: QMD index last updated 4 days ago; QMD itself suggests adding `qmd collection update-cmd` hooks.
  - Gap: no automated or manual index refresh was triggered in this run; policy for how often to re-embed memory/durable/outputs is still implicit rather than codified in cron.
- Navigation freshness vs. QMD navigation embedding.
  - Evidence: navigation refresh cron last ran successfully on 2026-06-09 at 04:00 America/New_York; QMD `codi-navigation` collection shows last update 6 days ago.
  - Gap: NAVIGATION.md files on disk are more recent than their QMD embeddings; no QMD update for navigation was triggered here.

## Exact current-run receipts

- `openclaw gateway status`
  - Service: LaunchAgent (loaded), running on 127.0.0.1:18789; CLI/Gateway version: 2026.6.5; connectivity probe OK; admin-capable.
- `openclaw gateway health`
  - Health: OK (18ms); Telegram configured; no degraded event-loop flags.
- `openclaw gateway stability`
  - Events: 1000/1000, dropped=355.
  - Types: diagnostic.heartbeat, diagnostic.memory.sample, session.state, queue.lane.enqueue/dequeue, exec.process.completed, message.queued, message.delivery.*.
  - Memory sample: rss=448 MiB, heap=360 MiB, pressure=0.
- `openclaw gateway call cron.list`
  - Jobs: 12 total; all enabled.
  - Key jobs and statuses:
    - `Codi repo commit workflow`: lastRunStatus `ok`, nextRunAt set.
    - `Codi operational queue check`: lastRunStatus `ok`.
    - `Codi Moltbook Daily Report`: lastRunStatus `ok`.
    - `Codi Moltbook Reading Pulse`: lastRunStatus `ok`.
    - `Codi Moltbook Social Watcher`: lastRunStatus `error`, lastDiagnosticSummary `Error: Unknown system error -11: Unknown system error -11, read`, `consecutiveErrors: 1`.
    - `Codi xos-nightly-maintenance DEFAULT MODEL`: this job; lastRunStatus `ok`, currently running with nextRunAt 2026-06-11 02:00 America/New_York.
    - `Codi durable memory promotion`: lastRunStatus `ok`, delivery `delivered`.
    - `Codi XLR8ROS navigation tree refresh`: lastRunStatus `ok`.
    - `Codi repo junk cleanup`: lastRunStatus `ok`.
    - `CodiCore daily stale-file staging`: lastRunStatus `ok`.
    - `Codi Moltbook Editorial Initiation`: lastRunStatus `ok`.
    - `CodiCore weekly office archive transfer`: lastRunStatus `ok`.
- Doctor/state-migrations warnings
  - "Left plugin install index in place because shared SQLite state has conflicting plugin install metadata for: codex, manifest".
- `qmd status`
  - Index: `/Users/reginaldberry/.cache/qmd/index.sqlite` (~55.7 MB).
  - Documents: 114; vectors: 5704; last updated: 4 days ago.
  - Collections:
    - `codi-memory`: 62 files; context: active daily-note evidence.
    - `codi-durable-memory`: 25 files; context: promoted durable memory.
    - `codi-howto`: 0 files.
    - `codi-outputs`: 26 files.
    - `codi-navigation`: 1 file; updated 6 days ago.
  - Models: embedding (embeddinggemma-300M-GGUF), reranking (Qwen3-Reranker-0.6B-Q8_0-GGUF), generation (qmd-query-expansion-1.7B-gguf); device: Apple M1 Metal, 11.8 GB VRAM free.
- `qmd vsearch --help`
  - Confirms vector-only search command and related options are present.
- `qmd embed --help`
  - Confirms embedding maintenance command and its batch/size options.
- `qmd query 'health check test' -n 3`
  - Returned 3 results from `codi-memory`, including entries around health checks and nightly maintenance; printed a list of many `memory/YYYY-MM-DD.md` files plus `migration-backups/` and `2026-06-10` subdir, confirming indexed corpus coverage.
- `ls maintenance`
  - Shows historical nightly maintenance files from 2026-05-16 onward, a JSON artifact for 2026-06-09, and `2026-06-10-nightly.md`.

---

Maintenance log path for this run: `maintenance/2026-06-10-nightly.md`.
