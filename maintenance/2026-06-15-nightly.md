# XOS Nightly Maintenance — 2026-06-15

Time: 2026-06-15 17:32 UTC (2026-06-15 13:32 America/New_York)

## 1. What was checked (current run)

- OpenClaw gateway health (service + connectivity)
- OpenClaw overall status (including tasks/cron summary)
- Active task/cron health (recent cron runs from OpenClaw tasks list)
- Failed or skipped cron runs (from tasks list)
- Durable memory promotion health (latest cron promotion run receipts)
- QMD memory/search health (status, embed dry-run, search)
- Navigation freshness (latest navigation cron receipt)
- Repo/workspace maintenance indicators (junk cleanup, commit workflow, stale-file staging)

## 2. What passed (verified facts)

### OpenClaw gateway / status
- `openclaw gateway status` reports:
  - Gateway service: LaunchAgent installed, loaded, running (pid 10240, state active).
  - Bind: loopback-only (127.0.0.1) on port 18789; dashboard reachable at http://127.0.0.1:18789/.
  - CLI and gateway version: 2026.6.5.
  - Connectivity probe: ok.
  - Capability: admin-capable.
- `openclaw status` (fast) reports:
  - OS: macOS 26.3.1 (arm64), node 22.22.2.
  - Gateway: local, reachable, with auth token.
  - Gateway service: LaunchAgent installed · loaded · running (pid 10240, state active).
  - Tasks: 9 active · 0 queued · 9 running · 9 issues · audit clean · 112 tracked.
  - Heartbeat: 30m (main), disabled (codi).

Conclusion: gateway and core OpenClaw service are healthy and reachable.

### Active task/cron health
- `openclaw tasks list --json` (via process log) shows the most recent cron runs for Codi-related jobs.
- For the latest nightly batch around 2026-06-09 (reference from task receipts):
  - `Codi Moltbook Social Watcher` — status: succeeded.
  - `Codi Moltbook Reading Pulse` (two runs) — status: succeeded.
  - `Codi Moltbook Daily Report` — status: succeeded.
  - `Codi Moltbook Editorial Initiation` — status: succeeded (with external Moltbook post blocked by curl/network, but cron itself completed and reported).
  - `Codi XLR8ROS navigation tree refresh` — status: succeeded (with internal script blocker noted under issues below).
  - `Codi repo junk cleanup` — status: succeeded (informational, blocked on host exec in that run; see issues).
  - `Codi repo commit workflow` — status: succeeded (informational, blocked on host exec in that run; see issues).
  - `Codi operational queue check` — status: succeeded (terminalSummary: `NO_REPLY`).
  - `CodiCore daily stale-file staging` — status: succeeded; staged 116 stale files into a tarball under `cleanup-staging/20260610T012656Z-staging/` with manifest.
  - `Codi durable memory promotion` — status: succeeded.

Conclusion: All listed maintenance cron jobs themselves are reporting status `succeeded` except for the specific nightly maintenance run noted under failures. Several jobs succeeded but reported internal blockers that still need follow-up.

### Durable memory promotion health
- Latest `Codi durable memory promotion` cron run receipt:
  - Status: succeeded.
  - promoted durable file path: `Durable_Memory/2026-06-09.md`.
  - number of entries promoted: 0 (no 2026-06-09 daily-note events existed at promotion time).
  - promotion log path: `Outputs/promotion-logs/2026-06-09-durable-memory-promotion-log.md`.
  - QMD/indexing blocker: none (QMD/indexing was not invoked and no errors occurred).
  - write blocker: none (durable memory file, promotion log, and daily note entry for the promotion were written).

Conclusion: durable memory promotion machinery is functioning; this run had nothing to promote because the source daily-note lane was empty for that date.

### QMD memory/search health
- `qmd status` reports:
  - Index: `/Users/reginaldberry/.cache/qmd/index.sqlite`, size ~55.7 MB.
  - Documents: 150 files indexed; 6644 vectors embedded; last updated ~1 day ago.
  - Collections:
    - `codi-memory`: pattern `**/*.md`, 71 files, updated 1 day ago.
    - `codi-durable-memory`: pattern `**/*.md`, 32 files, updated 2 days ago.
    - `codi-howto`: 0 files (never updated yet).
    - `codi-outputs`: 46 files, updated 1 day ago.
    - `codi-navigation`: 1 file, updated 5 days ago.
  - Models configured for embedding, reranking, and generation are all present and active.
- `qmd embed --dry-run .` reports:
  - Workdir path under iCloud Drive was unavailable, so QMD fell back to `~/.openclaw`.
  - Result: `✓ All content hashes already have embeddings.` (no missing embeddings at this time for the active index view).
- `qmd search "test query" -c codi-memory` returns multiple results from `codi-memory` (daily-note evidence) with non-zero scores, confirming retrieval.

Conclusion: QMD is healthy for status, embedding, and retrieval. Collections are populated and recent; dry-run embed indicates no pending embeddings for the currently visible content set.

### Navigation freshness
- From `openclaw tasks list --json` current-run receipts:
  - `Codi XLR8ROS navigation tree refresh`:
    - Status: succeeded.
    - Terminal summary notes that NAVIGATION.md paths were updated: `none`.
    - Blocker: script failed in a prior run because ROOT was set to `/Users/reginaldberry/Library Mobile Documents/com~apple~CloudDocs/XLR8ROS` (missing `~/Library` tilde expansion / space), which does not exist; caused FileNotFoundError.
- For this maintenance pass, we only have that receipt; no new navigation refresh was executed directly.

Conclusion: navigation cron is running and reporting, but the underlying refresh script previously had a path configuration bug. This is an unresolved issue to fix in the script configuration, not a live crash in this run.

### Repo/workspace health indicators
- From current-run cron receipts:
  - `Codi repo junk cleanup`:
    - Status: succeeded.
    - Terminal summary: could not actually inspect or delete files because host exec was denied (`exec host not allowed; configured host is gateway-only`). The job produced only a planned cleanup pattern and suggested host commands.
  - `Codi repo commit workflow`:
    - Status: succeeded for the cron, but all repos (CodiCore, XLR8ROS-HQ, XLR8ROS_SEAD, HOW-2-HUB) remained in review hold due to the same host exec restriction. No git status/add/commit or file inspections were performed.
  - `CodiCore daily stale-file staging`:
    - Status: succeeded.
    - 116 stale files (≥72h) were staged into `cleanup-staging/20260610T012656Z-staging/stale-bundle.tar.gz` with a TSV manifest classifying file types. Uncertain items were intentionally left in place.

Conclusion: workspace-level autosanitation and staging are functioning at the level of classification and bundling, but actual repo-root cleanup and commit workflows are currently blocked by host exec restrictions.

## 3. What failed or is partially failing (unresolved issues)

### 3.1 Nightly maintenance cron failure
- `Codi xos-nightly-maintenance` (prior run) in `openclaw tasks list`:
  - Status: failed.
  - Error: `⚠️ 🛠️ \`create folder maintenance → list files (in ~/Library/Mobile Documents/com~apple~CloudDocs/XLR8ROS/Agents/Primary/CodiCore/CodiCore)\` failed`.
  - Terminal summary matches the error string.
- Interpretation: the prior nightly maintenance cron run could not create or list the `maintenance` folder under the CodiCore workspace. The exact cause (permissions vs. path vs. existence) is not visible in this receipt alone, but the failure is recorded.

Current-run status: This run successfully wrote `maintenance/2026-06-15-nightly.md`, so the immediate folder-creation problem appears resolved for now. However, the previous failure remains as a historical issue and should be considered when reviewing log continuity.

### 3.2 Navigation refresh script path bug (historical but not yet fixed)
- Receipt from `Codi XLR8ROS navigation tree refresh`:
  - Blocker: ROOT configured as `/Users/reginaldberry/Library Mobile Documents/com~apple~CloudDocs/XLR8ROS`, which does not exist; caused FileNotFoundError when attempting to iterate the directory.
- Status for that cron run is still `succeeded`, but semantically it indicates that the refresh did not update any NAVIGATION paths.

Unresolved: underlying script configuration still needs correction to the actual iCloud path (`/Users/reginaldberry/Library/Mobile Documents/...`).

### 3.3 Repo commit and junk cleanup blocked by host exec restrictions
- `Codi repo commit workflow` and `Codi repo junk cleanup` both report:
  - Blocker: tools.exec cannot run on the host where the XLR8ROS tree lives (`exec host not allowed (requested auto; configured host is gateway; set tools.exec.host=auto to allow this override)`).
  - Consequence: no git operations, file listings, or deletions were performed. All repos remain effectively in review hold for these runs.

Unresolved: either grant safe host exec for these maintenance agents or keep treating these jobs as planning-only and adjust expectations.

### 3.4 Moltbook DM endpoint instability
- `Codi Moltbook Social Watcher` current-run receipt:
  - Status: succeeded.
  - Blocker note: DM check endpoint returned `404 Not Found`; DM status cannot be verified.

Unresolved: DM-check API path or configuration needs review. This is external to core XOS filesystem but relevant to social-watcher health.

## 4. Exact current-run receipts referenced

- `openclaw gateway status` — service running, loopback-only binding, pid 10240, version 2026.6.5, connectivity probe ok.
- `openclaw status` — gateway reachable, LaunchAgent running, 9 active tasks with 9 issues, heartbeat main=30m, codi heartbeat disabled.
- `openclaw tasks list --json` — used to inspect cron tasks and their `status`, `error`, and `terminalSummary` fields for:
  - `Codi xos-nightly-maintenance` (failed; maintenance folder create/list error).
  - `Codi Moltbook Social Watcher` (succeeded; DM 404 blocker noted).
  - `Codi Moltbook Reading Pulse` (succeeded; reading logs only).
  - `Codi Moltbook Daily Report` (succeeded; explicit “no new activity” report).
  - `Codi Moltbook Editorial Initiation` (succeeded; posting blocked by curl/network resolution error).
  - `Codi XLR8ROS navigation tree refresh` (succeeded; ROOT path bug, no NAVIGATION updates).
  - `Codi repo junk cleanup` (succeeded; host exec blocked, no actual deletions).
  - `Codi repo commit workflow` (succeeded; host exec blocked, all repos in review hold).
  - `Codi operational queue check` (succeeded; `NO_REPLY`).
  - `CodiCore daily stale-file staging` (succeeded; 116 files staged to tarball with manifest).
  - `Codi durable memory promotion` (succeeded; 0 entries promoted; durable file/log paths recorded; no QMD blockers).
- `qmd status` — confirmed index location, collection membership and freshness, model availability, and device info.
- `qmd embed --dry-run .` — reported that all content hashes already have embeddings for the active view.
- `qmd search "test query" -c codi-memory` — returned concrete hits from `codi-memory`, validating retrieval end-to-end.
- `write maintenance/2026-06-15-nightly.md` — this maintenance log file was created successfully in the CodiCore workspace.

## 5. Summary

- Core OpenClaw gateway and status surfaces are healthy.
- QMD indexing and retrieval are healthy; no missing embeddings reported for the active collections.
- Durable memory promotion pipeline is working and wrote logs successfully; the last run simply had no events to promote.
- Cron/task framework is running, with 9 active tasks and several jobs reporting `succeeded` but with embedded blockers.
- Historical issues remain around:
  - A previous nightly maintenance cron failure when creating/listing the `maintenance` folder.
  - A navigation refresh script path bug pointing at a non-existent XLR8ROS root.
  - Repo commit and junk-cleanup jobs being blocked by host exec restrictions.
  - Moltbook DM endpoint returning 404 in the social watcher.
- This run successfully added a compact nightly maintenance log at `maintenance/2026-06-15-nightly.md`, confirming that the `maintenance` directory is now writable from the current agent context.
