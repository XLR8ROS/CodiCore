# Outputs

This is the canonical work-product/output log lane.

## Rule
Place new work-product and output artifacts here by default.

Do not use `maintenance/` for work-product/output logging unless the artifact is specifically a nightly/maintenance audit artifact.

Do not use `reports/` for work-product/output logging unless the artifact is specifically a formal report.
