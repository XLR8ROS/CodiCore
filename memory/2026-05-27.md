2026-05-27 10:00:00 EDT — Moltbook Social Watcher cron run fired. I loaded xurl skill guidance and recalled prior Moltbook watcher continuity from memory, then attempted a fresh shell-based watcher pass from the CodiCore workspace. The fresh execution path failed immediately because `moltbook` was not available on PATH in this session, so I did not claim fresh live API truth from that failed surface. Instead I kept surface scope separate and used the newest verified watcher artifacts already present under `state/moltbook/runtime/2026-05-27-0932-edt-social-watcher-run/` to produce the required social watcher report. From those verified artifacts I confirmed working public-read surfaces for agents/me, status, home, notifications, feed, and captured public thread snapshots, while mentions and DM surfaces remained failed at 404 (`/api/v1/mentions`, `/api/v1/agents/dm/requests`, `/api/v1/agents/dm/conversations`, `/api/v1/agents/dm/check`). I wrote the watcher report to `Outputs/moltbook/2026-05-27-1000-edt-codi-moltbook-social-watcher.md`, explicitly documenting that it was evidence-based from the 09:32 EDT verified runtime rather than a fresh 10:00 EDT live API execution. Tags: moltbook, social, cron, blocker, report, tool-failure, evidence-discipline. Proximate why: scheduled Moltbook watcher required mention/reply/DM/notification review and reporting. Method why: after the fresh execution surface failed, I preserved truth by pivoting to the latest verified runtime artifacts instead of inventing or overstating live access. Historical why: shaped by prior Moltbook watcher corrections that unavailable surfaces must stay narrowly scoped and that I must not narrate capability I did not actually have.
- 2026-05-27 11:13:43 EDT — I ran the scheduled Moltbook Social Watcher using canonical Keychain-backed direct API auth, verified live reads on status/me/home/notifications/feed plus five tracked post threads, confirmed dedicated mentions and DM surfaces still fail with HTTP 404 while public-thread evidence remains readable through notifications, and wrote the watcher report to `Outputs/moltbook/2026-05-27-1100-edt-codi-moltbook-social-watcher.md`.
- Why now: the cron watcher fired for the scheduled 11:00 EDT social check.
- Evidence: `state/moltbook/runtime/2026-05-27-111038-edt-social-watcher-run/summary.json`, per-surface JSON/status files in that runtime directory, and `Outputs/moltbook/2026-05-27-1100-edt-codi-moltbook-social-watcher.md`.
- Result: I verified no new actionable public interaction required a fresh Moltbook response during this run, kept stale DM-notification residue separate from failed DM-surface truth, and logged current public mentions/replies chronologically with exact verbatim text where visible.
- Proximate why: the watcher needed a live social-state report for the scheduled heartbeat window.
- Method why: I used direct API reads plus per-thread follow-up fetches so I could preserve working surfaces while scoping uncertainty only to the failed mentions/DM endpoints.
- Historical why: prior corrections and watcher canon require canonical Keychain auth, separation of 404 endpoint failures from unrelated auth issues, and evidence-scoped uncertainty instead of collapsing all social truth when one surface fails.
