# 2026-04-29

## Google / gog status (fixed)
- Confirmed Google OAuth is now operational for `gog` on this Mac.
- Credentials file exists at `~/Library/Application Support/gogcli/credentials.json` (client_id + client_secret present).
- Refresh token exists in macOS Keychain under service `gogcli`, account `token:admin@xlr8ros.com`.
- Earlier failures were due to Keychain secret reads hanging (likely waiting on an interactive Keychain “Allow/Always Allow” prompt).
- After user attempted to clear the stuck UI prompt, `gog` became able to read the token non-interactively again.
- Read-only smoke tests succeeded:
  - Gmail search returned recent messages.
  - Calendar calendars list returned calendars.
  - Drive `ls` returned files/folders.
- One calendar events command attempt failed due to wrong flag (`--time-min` not supported on this gog version); not an auth issue.

## Manifest / Port issue (bug-report mode)
- Manifest server is healthy on `127.0.0.1:2099`, but doctor/status flows can still trigger an extra startup attempt causing `EADDRINUSE`.
- Clean reinstall + registry refresh did not eliminate the issue, indicating likely plugin/runtime bug.
- Bug report drafted for maintainers: `manifest-openclaw-bug-report.md`.
- Raw evidence capture generated: `/tmp/manifest-openclaw-bug-report-evidence.txt`.

## Skills / Doctor hygiene
- Ran `openclaw skills check`: total 56; eligible 32; missing requirements 24 initially.
- Fixed `session-logs` skill by installing `ripgrep` (`rg`) via Homebrew (jq already present).
- After install: missing requirements dropped to 23; `session-logs` is ready.

## What I learned today (operational)
- In this environment, issues that look like “missing tokens” can actually be macOS Keychain access-control prompts blocking non-interactive CLI runs.
- When gog/Keychain reads hang with no output, treat it as a UI permission prompt problem first (remote desktop/Screens needed), not as missing credentials.
- `openclaw skills check` is the canonical way to audit skill readiness; address the highest-value missing bins first instead of installing everything.
- General rule reinforced by user: learn from what works and what doesn’t; successful fixes and failed approaches should both update future judgment, not just immediate status.
