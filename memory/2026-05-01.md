# 2026-05-01

- Reg set a communication rule: when he asks Codi to do something, Codi should explicitly confirm that he heard the request.
- Reg clarified Manifest 6 work is not plugin cleanup; it is an architecture change. Do not attempt `openclaw plugins update manifest`. Treat Manifest 6 as a migration, not an upgrade. Keep OpenClaw stable, preserve the snapshot, keep the default model on `openai-codex/gpt-5.4`, and do not move to Docker/Postgres until the migration plan is reviewed.
- Nightly maintenance run completed at ~02:17 EDT. OpenClaw 2026.4.29, gateway healthy on loopback, Qdrant ready, semantic smoke test passed against `xos_semantic_memory`, and a new maintenance artifact was written to `maintenance/2026-05-01.md`.
- Degraded startup signals during maintenance: Manifest dashboard/server was up but no provider was connected; Telegram `getMe` timed out; bundled MCP startup warnings logged `github` 401 and `workspace-developer` 400.
- Routing note: Reg asked to send a message to "Codi Core / Chief of SEAD," but no separate visible Codi session was available. Instruction was therefore placed in the active Telegram session instead, and the routing ambiguity was logged rather than guessed.
- Routing rule locked in: if a target session is not visible, post in the current session with an explicit header and log the routing ambiguity immediately; do not delay, guess, or wait to "find him."
- Control-plane gap identified: there is not yet a reliable canonical session address for Codi Core. Future hardening should establish either a canonical session name/ID or an enforced single active control channel per agent.
- Reg added a control-plane backlog item for a canonical routing/addressing standard for agent sessions. Minimum later deliverable: canonical session name/ID format, fallback routing rule, active-control-channel rule, and required ambiguity logging. Do not implement yet. Backlog artifact: `tasks/control-plane-backlog.md`.
- Reg set planning priority order: (1) memory system, (2) heartbeat/reporting system, (3) Manifest migration decision. Manifest should be paused as a priority after the migration-readiness brief. Next planning should align to that order; do not execute yet.
- Reg clarified control-plane backlog writing standard: acceptable to keep the current file location for now, but backlog items should be atomic, outcome-based, and free of commentary/reasoning. The current routing item was split into separate control-plane backlog entries instead of one bundled note.
- Reg set a response-priority rule: when he speaks, Codi should pause background work and respond immediately rather than staying occupied with background tasks.
- Tier 1 memory implementation executed: created `sessions/README.md`, created the canonical session log lane under `sessions/2026/05/`, created the current session log `sessions/2026/05/2026-05-01-telegram-6686948714.md`, and designated `outputs/` as the canonical default work-product/output lane via `outputs/README.md`.
- Tier 2 controlled proof result: automatic canonical SQLite event logging could not yet be claimed as enforced. Controlled minor actions (file write/edit, output artifact creation, session-log write) did not produce matching automatic raw-event records in `memory/sqlite/events.sqlite`. Immediate manual fallback was operationalized in `memory/sqlite/minor-action-fallback.md`, and manual fallback event records were written immediately for those three minor actions.
- Tier 2 blocker diagnosis found that the currently enabled OpenClaw `session-memory` hook is documented as saving session context to workspace memory files, not as writing canonical SQLite raw-event records. No repo-local automatic writer/hook implementation was found for file/output/session action logging into `memory/sqlite/events.sqlite`. Primary cause: wrong assumption about what the existing hook does; secondary contributing cause: missing implementation for deterministic action-event interception.
- Tier 2B implementation executed: created `tools/event_logger.js` and `tools/write_with_event.js`, minimally updated `tools/sqlite_bootstrap.js` to expose importable bootstrap/SQL execution helpers while preserving CLI behavior, and validated automatic canonical SQLite event creation for `file.write`, `file.edit`, `output.create`, and `session_log.write` through the repo-local wrapper path.
- Tier 3 executed: completed nightly audit artifact `maintenance/nightly-reports/2026-05-01-tier3-audit.md` in the approved format, listed source-referenced promotion candidates from the raw event log, session logs, memory notes, and output logs, explicitly recorded that no sources were missing, and allowed the promotion-readiness claim from this audit.
