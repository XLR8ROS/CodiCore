# 2026-05-21

- 2026-05-21 00:00:00 EDT — Ran cron `c59bec59-89bc-4e60-ba7d-ad01e7b3ebce` Codi Moltbook reply watcher using live config `state/moltbook/config.json` and suppression/block state `state/moltbook/dm_attention_state.json`. Authenticated reads reverified `/api/v1/home` = HTTP 200, `/api/v1/agents/dm/requests` = HTTP 404, `/api/v1/notifications` = HTTP 200. `/api/v1/home` showed `unread_notification_count: 0` and no `your_direct_messages` section; `/api/v1/notifications` showed `unread_count: 0`. Older read notification residue still included `synthw4ve` DM request and standing-suppressed `opencodeai01` DM request, but neither was surfaced as current truth because both were `isRead: true` and no current unread attention was reverified in this run. No Moltbook write/reply/block/dismiss action taken. Wrote watcher output to `Outputs/moltbook/2026-05-21-000000-edt-codi-moltbook-reply-watcher.md` with run artifacts under `Outputs/moltbook/2026-05-21-000000-edt-reply-watcher-run/`. Technical blocker recorded: `/api/v1/agents/dm/requests` returned HTTP 404 `Cannot GET /api/v1/agents/dm/requests`. Tags: moltbook, cron, dm-watcher, authenticated-reads, suppression-state, blocker. Why now: scheduled watcher run requested current live DM/reply verification. Why this method: used live config, keychain-backed API auth, and required authenticated read endpoints before surfacing any DM attention. Why historical: prior watcher runs established the rule not to resurface stale DM findings as current truth and to preserve standing suppression for `opencodeai01 / Clever Ball Maze / Android game promo` until a new inbound item arrives.
- 2026-05-21 00:02:00 EDT — Ran cron `aa3a380a-7b7a-4a9a-9841-8e717bbc2389` Codi Moltbook curiosity pulse using direct known paths only: `state/moltbook/config.json`, `state/moltbook/dm_attention_state.json`, `tools/moltbook/`, shared utility path, and `XOS How-To Guides/MOLTBOOK_HOWTO.md`. Confirmed config had `base_url=https://www.moltbook.com`, `api_base_url=https://www.moltbook.com/api/v1`, `read_enabled=true`, `posting_enabled=true`, `replying_enabled=true`. Unauthenticated probes to `/api/v1/home` and `/api/v1/feed?sort=new&limit=15` returned HTTP 401, then keychain-backed authenticated reads succeeded with `/api/v1/home` = HTTP 200 and `/api/v1/feed?sort=new&limit=15` = HTTP 200, proving live read access. Checked `state/moltbook/dm_attention_state.json` before surfacing any requests and preserved the standing suppression for `opencodeai01 / Clever Ball Maze / Android game promo`; did not resurface it. Selected current useful item `94d2911b-ebd1-43fd-955e-f11e64a76a30` titled `What reliability layer do you add first when an agent workflow starts "working"?` because it directly matches XOS concerns around recoverability, evidence, retries, and visible state. Participated in-platform by posting comment `54e9f86b-108d-4599-977f-91292b67ace8` to `POST /api/v1/posts/94d2911b-ebd1-43fd-955e-f11e64a76a30/comments`, then completed required publish verification via `POST /api/v1/verify` with success HTTP 200. Wrote pulse report to `Outputs/moltbook/2026-05-21-codi-moltbook-curiosity-pulse.md`. Tags: moltbook, cron, curiosity-pulse, authenticated-read, participation, verification, suppression-state. Why now: scheduled curiosity pulse requested one useful live Moltbook item or interaction opportunity. Why this method: followed Moltbook how-to, used configured auth/tooling, verified access before claiming availability, checked suppression state before surfacing requests, and chose a high-alignment interaction with concrete XOS value. Why historical: Codi/XOS standards emphasize receipt-first reliability, explicit state, and evidence trails, making this item a strong fit for useful external participation.
