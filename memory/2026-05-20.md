## 2026-05-20T02:31:56-04:00
- Event: Ran cron `c59bec59-89bc-4e60-ba7d-ad01e7b3ebce` Codi Moltbook reply watcher using the current live Moltbook config and authenticated reads to `/api/v1/home`, `/api/v1/agents/dm/requests`, and `/api/v1/notifications`.
- Evidence: Reverified `state/moltbook/config.json` remained write-enabled with `mode=active`, `read_enabled=true`, `replying_enabled=true`, `posting_enabled=true`, and `dm_watch_enabled=true`.
- Evidence: Fresh authenticated reads for the three required endpoints all returned HTTP 200 and were written under `Outputs/moltbook/2026-05-20-023156-edt-reply-watcher-run/` with `summary.json` showing `home_pending_request_count: "0"`, `incoming_count: 0`, `outgoing_count: 0`, `incoming_requests: []`, and `notification_unread_count: 0`.
- Evidence: Notifications still contained historical read `dm_request` items for `synthw4ve` and `opencodeai01`, but no unread notifications and no live inbound requests were present in this run, so those historical items were not surfaced as current truth.
- Finding: No current replies or DM requests needed attention in this run because authoritative live DM state remained zero.
- Finding: Standing blocked/suppressed item `opencodeai01 / Clever Ball Maze / Android game promo` remained honored and was not resurfaced.
- Result: No in-platform write, reply, ignore, block, dismiss, delete, or remove action taken.
- Output: `Outputs/moltbook/2026-05-20-023156-edt-codi-moltbook-reply-watcher.md`
- Technical blocker: configured suppression state file path `state/moltbook/dm_attention_state.json` was missing in the current workspace, so suppression handling relied on the standing rule already established in prior runs and the current instruction rather than a fresh file read.
- Tags: moltbook, dm-watcher, authenticated-read, suppression-state, no-action, technical-blocker
- Why now: Scheduled cron watcher requested a fresh live verification cycle at 02:30 EDT.
- Why this method: Re-read the required authenticated endpoints with current live config, used `/api/v1/agents/dm/requests` and `/api/v1/home` as authoritative current-state signals, and avoided resurfacing stale notification residue as current truth.
- Historical why: Reg required pending inbound DMs to be surfaced until addressed, stale prior DM findings not to be resurfaced as current truth unless reverified in the same run, and the standing `opencodeai01 / Clever Ball Maze / Android game promo` item to remain blocked/suppressed.
## 2026-05-20T02:32:00-04:00
- Event: Completed nightly durable-memory promotion workflow for the current run window and wrote the dated durable memory record plus promotion log.
- Evidence: Promoted canonical daily-note entries from `memory/2026-05-19.md` and `memory/2026-05-20.md` into `Durable_Memory/2026-05-20.md`; wrote log `Outputs/promotion-logs/2026-05-20-durable-memory-promotion-log.md`.
- Result: Promoted 21 entries total; skipped none; QMD/index refresh remained unavailable in the current toolset and the expected HOWTO path `IMPORTANT_CODI_HOW-TO/XOS_Memory_Flow_HOWTO.md` was missing at the workspace path, so direct-source promotion proceeded without indexing refresh.
- Tags: durable-memory, promotion, promotion-log, qmd-blocker, howto-path-drift
- Why now: Scheduled nightly durable-memory promotion cron required same-run promotion/write completion, not candidate-only review.
- Why this method: Used canonical daily notes as authority, verified one active daily note per date, promoted every reviewed entry into the dated durable-memory record, and logged retrieval/indexing limitations separately from write completion.
- Historical why: Reg's standing doctrine says promotion is not an importance contest, every canonical daily-note event gets a durable record, and QMD is retrieval support only rather than the durable-memory write path.
