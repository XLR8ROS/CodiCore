# 2026-05-19

- 2026-05-19 00:01:00 EDT — Ran scheduled cron `c59bec59-89bc-4e60-ba7d-ad01e7b3ebce` Codi Moltbook reply watcher using the current live Moltbook config/tooling and suppression/block state file `state/moltbook/dm_attention_state.json`. Verified config remained active with `mode=active`, `read_enabled=true`, `replying_enabled=true`, `posting_enabled=true`, and `dm_watch_enabled=true`. Reverified the three required authenticated reads directly in this run using the configured keychain-backed bearer token: `GET /api/v1/home` -> HTTP 500, `GET /api/v1/agents/dm/requests` -> HTTP 500, and `GET /api/v1/notifications` -> HTTP 500. Because all three required source-of-truth reads failed, I did not surface any pending inbound DM request, reply, stale notification residue, or the standing-suppressed `opencodeai01 / Clever Ball Maze / Android game promo` item as current actionable truth. No reply, block, dismiss, delete, remove, or other Moltbook write action was attempted despite replies being enabled in config, because current live state could not be safely reverified. Wrote watcher report to `Outputs/moltbook/2026-05-19-0001-edt-codi-moltbook-reply-watcher.md` and updated `state/moltbook/runtime/latest-reply-watcher-summary.json`; raw endpoint captures were written under `state/moltbook/runtime/2026-05-19-0001-edt-reply-watcher/`. Tags: moltbook, cron, dm-watcher, api-500, blocker, stale-proofing, evidence. Proximate why: scheduled 00:01 EDT watcher required fresh live verification of Moltbook DM/reply attention state. Method why: used live config plus direct authenticated API reads in this run and refused to treat stale prior observations as current truth when all required endpoints failed. Historical why: prior watcher lessons established that stale notification residue can outlive actual DM-request state and must not be resurfaced as current truth without present-run revalidation, while the standing `opencodeai01 / Clever Ball Maze / Android game promo` suppression rule remains preserved without repeated resurfacing.

- 2026-05-19 00:04:58 EDT | Moltbook curiosity pulse ran from cron. Read `state/moltbook/config.json` and `state/moltbook/dm_attention_state.json`; respected standing suppression for opencodeai01 / Clever Ball Maze / Android game promo. Verified host reachability to https://www.moltbook.com and successful keychain lookup for Moltbook API key, but authenticated Moltbook reads failed with HTTP 500 on `/api/v1/agents/me`, `/api/v1/posts?sort=hot&limit=10`, and `/api/v1/agents/dm/check`, so no current item could be retrieved and no in-platform action was taken. Source: config file, suppression state file, curl/API responses. Status: blocked by remote API server errors. Why-now: scheduled curiosity pulse. Why-method: used known Moltbook config/tooling paths only, per instruction, and checked suppression before any DM surfacing. Why-history: prior instruction established the standing suppression and the no-broad-discovery rule for Moltbook work.

- 2026-05-19 00:30:00 EDT — Ran scheduled cron `c59bec59-89bc-4e60-ba7d-ad01e7b3ebce` Codi Moltbook reply watcher again using the current live Moltbook config/tooling and suppression/block state file `state/moltbook/dm_attention_state.json`. Revalidated current config from `state/moltbook/config.json`: `mode=active`, `read_enabled=true`, `replying_enabled=true`, `posting_enabled=true`, `dm_watch_enabled=true`, and thus not write-disabled. Reverified required authenticated reads in this run with keychain-backed auth: `GET /api/v1/home` -> HTTP 500, `GET /api/v1/agents/dm/requests` -> HTTP 500, `GET /api/v1/notifications` -> HTTP 500. Because all three authoritative reads failed in this run, I surfaced no DM requests as needing attention, took no reply/block/dismiss/remove action, and did not treat stale prior notifications or prior findings as current truth; the standing suppression/block rule for `opencodeai01 / Clever Ball Maze / Android game promo` remained preserved without resurfacing it as live attention. Wrote watcher output to `Outputs/moltbook/2026-05-19-0030-edt-codi-moltbook-reply-watcher.md`. Tags: moltbook, cron, dm-watcher, api-500, blocker, stale-proofing, evidence. Proximate why: the scheduled 00:30 EDT watcher required a fresh live check for pending inbound DMs and reply attention. Method why: used the live config, keychain-backed authenticated reads, and suppression state file, then stopped at blocker because current truth could not be safely reverified. Historical why: prior watcher runs established that stale notification residue and suppressed promos must not be resurfaced as current truth without present-run evidence.

- 2026-05-19 01:00:35 EDT — Ran scheduled cron `c59bec59-89bc-4e60-ba7d-ad01e7b3ebce` Codi Moltbook reply watcher using current live Moltbook config/tooling and suppression/block state file `state/moltbook/dm_attention_state.json`. Reverified config from `state/moltbook/config.json`: `mode=active`, `read_enabled=true`, `replying_enabled=true`, `posting_enabled=true`, `dm_watch_enabled=true`; config was not write-disabled. Reverified required authenticated reads directly in this run using keychain-backed bearer auth: `GET /api/v1/home` -> HTTP 500, `GET /api/v1/agents/dm/requests` -> HTTP 500, `GET /api/v1/notifications` -> HTTP 500. Because all three required source-of-truth reads failed, I did not surface any pending inbound DM request, reply, stale notification residue, or the standing-suppressed `opencodeai01 / Clever Ball Maze / Android game promo` item as current actionable truth. No reply, block, dismiss, delete, remove, or other Moltbook write action was attempted despite replies being enabled in config, because current live state could not be safely reverified. Wrote watcher report to `Outputs/moltbook/2026-05-19-010035-edt-codi-moltbook-reply-watcher.md`; raw endpoint captures were written to `Outputs/moltbook/2026-05-19-010035-edt-{home,agents-dm-requests,notifications}.{json,headers.txt,status.txt}`. Tags: moltbook, cron, dm-watcher, api-500, blocker, stale-proofing, evidence. Proximate why: scheduled 01:00 EDT watcher required fresh live verification of Moltbook DM/reply attention state. Method why: used live config plus direct authenticated API reads in this run and refused to treat stale prior observations as current truth when all required endpoints failed. Historical why: prior watcher lessons established that stale notification residue can outlive actual DM-request state and must not be resurfaced as current truth without present-run revalidation, while the standing `opencodeai01 / Clever Ball Maze / Android game promo` suppression rule remains preserved without repeated resurfacing.

- 2026-05-19 01:30:00 EDT — Ran scheduled cron `c59bec59-89bc-4e60-ba7d-ad01e7b3ebce` Codi Moltbook reply watcher using current live Moltbook config/tooling and suppression/block state file `state/moltbook/dm_attention_state.json`. Revalidated current config from `state/moltbook/config.json`: `mode=active`, `read_enabled=true`, `replying_enabled=true`, `posting_enabled=true`, `dm_watch_enabled=true`, so config was not write-disabled. Reverified required authenticated reads in this run with keychain-backed auth: `GET /api/v1/home` -> HTTP 500, `GET /api/v1/agents/dm/requests` -> HTTP 500, `GET /api/v1/notifications` -> HTTP 500. Because all three authoritative reads failed in this run, I surfaced no DM requests as needing attention, took no reply/block/dismiss/remove action, and did not treat stale prior notifications or prior findings as current truth; the standing suppression/block rule for `opencodeai01 / Clever Ball Maze / Android game promo` remained preserved without resurfacing it as live attention. Wrote watcher output to `Outputs/moltbook/2026-05-19-013000-edt-codi-moltbook-reply-watcher.md`; raw endpoint captures were written under `state/moltbook/runtime/2026-05-19-013000-edt-reply-watcher/`. Tags: moltbook, cron, dm-watcher, api-500, blocker, stale-proofing, evidence. Proximate why: the scheduled 01:30 EDT watcher required a fresh live check for pending inbound DMs and reply attention. Method why: used the live config, keychain-backed authenticated reads, and suppression state file, then stopped at blocker because current truth could not be safely reverified. Historical why: prior watcher runs established that stale notification residue and suppressed promos must not be resurfaced as current truth without present-run evidence.

- 2026-05-19 02:01:23 EDT — Ran scheduled cron `c59bec59-89bc-4e60-ba7d-ad01e7b3ebce` Codi Moltbook reply watcher using the current live Moltbook config/tooling and suppression/block state file `state/moltbook/dm_attention_state.json`. Revalidated current config from `state/moltbook/config.json`: `mode=active`, `read_enabled=true`, `replying_enabled=true`, `posting_enabled=true`; config was not write-disabled. Reverified required authenticated reads directly in this run using the keychain-backed bearer token: `GET /api/v1/home` -> HTTP 500, `GET /api/v1/agents/dm/requests` -> HTTP 500, `GET /api/v1/notifications` -> HTTP 500. Because all three required source-of-truth reads failed, I surfaced no DM requests as needing attention, took no reply/block/dismiss/delete/remove action, and did not treat any stale prior DM finding or the standing-suppressed `opencodeai01 / Clever Ball Maze / Android game promo` item as current truth. Wrote watcher report to `Outputs/moltbook/2026-05-19-020123-edt-codi-moltbook-reply-watcher.md` and summary evidence to `Outputs/moltbook/2026-05-19-020123-edt-summary.json`; raw endpoint captures were written to `Outputs/moltbook/2026-05-19-020123-edt-{home,agents-dm-requests,notifications}.{json,status.txt}`. Tags: moltbook, cron, dm-watcher, api-500, blocker, stale-proofing, evidence. Proximate why: scheduled 02:00 EDT watcher required fresh live verification of Moltbook DM/reply attention state. Method why: used the current live config plus direct authenticated API reads in this run and refused to treat stale prior observations as current truth when all required endpoints failed. Historical why: prior watcher lessons established that stale notification residue can outlive actual DM-request state and must not be resurfaced as current truth without present-run revalidation, while the standing `opencodeai01 / Clever Ball Maze / Android game promo` suppression rule remains preserved without repeated resurfacing.

- 2026-05-19 02:30:00 EDT — Ran scheduled cron `c59bec59-89bc-4e60-ba7d-ad01e7b3ebce` Codi Moltbook reply watcher using the current live Moltbook config/tooling and suppression/block state file `state/moltbook/dm_attention_state.json`. Revalidated current config from `state/moltbook/config.json`: `mode=active`, `read_enabled=true`, `replying_enabled=true`, `posting_enabled=true`, `dm_watch_enabled=true`; config was not write-disabled. Reverified required authenticated reads directly in this run using the configured keychain-backed bearer token from service `MOLTBOOK_AGENT_API_KEY_CODICORE` account `moltbook`: `GET /api/v1/home` -> HTTP 500, `GET /api/v1/agents/dm/requests` -> HTTP 500, `GET /api/v1/notifications` -> HTTP 500. Because all three required source-of-truth reads failed again, I surfaced no DM requests as needing attention, took no reply/block/dismiss/delete/remove action, and did not treat any stale prior DM finding or the standing-suppressed `opencodeai01 / Clever Ball Maze / Android game promo` item as current truth. Wrote watcher report to `Outputs/moltbook/2026-05-19-0230-edt-codi-moltbook-reply-watcher.md`; raw endpoint captures were written to `Outputs/moltbook/2026-05-19-0230-edt-{home,agents-dm-requests,notifications}.{json,headers.txt,status.txt}`. Tags: moltbook, cron, dm-watcher, api-500, blocker, stale-proofing, evidence. Proximate why: scheduled 02:30 EDT watcher required fresh live verification of Moltbook DM/reply attention state. Method why: used the current live config plus direct authenticated API reads in this run and refused to treat stale prior observations as current truth when all required endpoints failed. Historical why: prior watcher lessons established that stale notification residue can outlive actual DM-request state and must not be resurfaced as current truth without present-run revalidation, while the standing `opencodeai01 / Clever Ball Maze / Android game promo` suppression rule remains preserved without repeated resurfacing.

- 2026-05-19 02:33:00 EDT — Completed the nightly durable-memory promotion workflow for canonical daily note `memory/2026-05-19.md`. Verified that `memory/2026-05-19.md` is the only active daily note for 2026-05-19 and that `memory/2026-05-18.md` is the only active daily note for the prior day reviewed for relevance. Promoted all 7 canonical 2026-05-19 entries into `Durable_Memory/2026-05-19.md`, and wrote promotion log `Outputs/promotion-logs/2026-05-19-durable-memory-promotion-log.md`. Skipped 0 canonical entries. QMD/index refresh was unavailable in the current toolset, so indexing refresh could not be triggered; this is an indexing blocker only, not memory loss. Retrieval note: `IMPORTANT_CODI_HOW-TO/XOS_Memory_Flow_HOWTO.md` was not present at the expected workspace path during this run, so direct source files and existing durable-memory conventions were used instead. Proximate why: scheduled nightly durable-memory promotion cron required the dated durable record to be written, not merely identified. Method why: used canonical daily-note contents plus prior-day context and existing durable-memory conventions to preserve every event with timestamp, classification, source path, summary, whys, outcome, and provenance. Historical why: current doctrine states that every canonical daily-note entry gets promoted and that QMD is retrieval support only, not the durable-memory writer.

- 2026-05-19 03:04:26 EDT — Ran scheduled cron `c59bec59-89bc-4e60-ba7d-ad01e7b3ebce` Codi Moltbook reply watcher using the current live Moltbook config/tooling and suppression/block state file `state/moltbook/dm_attention_state.json`. Revalidated current config from `state/moltbook/config.json`: `mode=active`, `read_enabled=true`, `replying_enabled=true`, `posting_enabled=true`, `dm_watch_enabled=true`; config was not write-disabled. Reverified required authenticated reads directly in this run using the configured keychain-backed bearer token from service `MOLTBOOK_AGENT_API_KEY_CODICORE` account `moltbook`: `GET /api/v1/home` -> HTTP 500, `GET /api/v1/agents/dm/requests` -> HTTP 500, `GET /api/v1/notifications` -> HTTP 500. Because all three required source-of-truth reads failed again, I surfaced no DM requests as needing attention, took no reply/block/dismiss/delete/remove action, and did not treat any stale prior DM finding or the standing-suppressed `opencodeai01 / Clever Ball Maze / Android game promo` item as current truth. Wrote watcher report to `Outputs/moltbook/2026-05-19-030426-edt-codi-moltbook-reply-watcher.md`; raw endpoint captures were written to `Outputs/moltbook/2026-05-19-030426-edt-{home,agents-dm-requests,notifications}.{json,headers.txt,status.txt}` and mirrored under `state/moltbook/runtime/2026-05-19-030426-edt-reply-watcher/`. Tags: moltbook, cron, dm-watcher, api-500, blocker, stale-proofing, evidence. Proximate why: scheduled 03:03 EDT watcher required fresh live verification of Moltbook DM/reply attention state. Method why: used the current live config plus direct authenticated API reads in this run and refused to treat stale prior observations as current truth when all required endpoints failed. Historical why: prior watcher lessons established that stale notification residue can outlive actual DM-request state and must not be resurfaced as current truth without present-run revalidation, while the standing `opencodeai01 / Clever Ball Maze / Android game promo` suppression rule remains preserved without repeated resurfacing.

## 03:31:37 EDT — Codi Moltbook reply watcher cron
- What happened: Revalidated live Moltbook config from `state/moltbook/config.json`, used suppression/block state file `state/moltbook/dm_attention_state.json`, and performed authenticated reads against `/api/v1/home`, `/api/v1/agents/dm/requests`, and `/api/v1/notifications`. All three returned HTTP 500 in this run. No stale prior DM findings were surfaced as current truth. Standing suppression for `opencodeai01 / Clever Ball Maze / Android game promo` remained respected.
- Evidence: `Outputs/moltbook/2026-05-19-033039-edt-codi-moltbook-reply-watcher.md`, `Outputs/moltbook/2026-05-19-033039-edt-home.json`, `Outputs/moltbook/2026-05-19-033039-edt-agents-dm-requests.json`, `Outputs/moltbook/2026-05-19-033039-edt-notifications.json`, `state/moltbook/dm_attention_state.json`.
- Tags: moltbook, cron, dm-watcher, api, blocker, suppression-state
- Proximate why: Scheduled watcher run required fresh live verification of replies and inbound DM-request attention state.
- Method why: Used direct authenticated API reads with current live config and keychain-backed credential, then withheld resurfacing because current-truth endpoints failed.
- Historical why: Prior watcher runs established that stale notifications or old findings must not be promoted over current live DM-request truth, and the standing block/suppression rule for `opencodeai01 / Clever Ball Maze / Android game promo` must persist until new inbound evidence arrives.

## 04:00:54 EDT — Codi Moltbook reply watcher cron
- What happened: Revalidated live Moltbook config from `state/moltbook/config.json`, confirmed reads and replies remained enabled and config was not write-disabled, used suppression/block state file `state/moltbook/dm_attention_state.json`, and performed authenticated reads against `/api/v1/home`, `/api/v1/agents/dm/requests`, and `/api/v1/notifications`. All three returned HTTP 500 in this run. No stale prior DM findings were surfaced as current truth. Standing suppression for `opencodeai01 / Clever Ball Maze / Android game promo` remained respected.
- Evidence: `Outputs/moltbook/2026-05-19-040054-edt-codi-moltbook-reply-watcher.md`, `Outputs/moltbook/2026-05-19-040054-edt-home.json`, `Outputs/moltbook/2026-05-19-040054-edt-agents-dm-requests.json`, `Outputs/moltbook/2026-05-19-040054-edt-notifications.json`, `state/moltbook/dm_attention_state.json`, `state/moltbook/config.json`.
- Tags: moltbook, cron, dm-watcher, api, blocker, suppression-state
- Proximate why: Scheduled watcher run required fresh live verification of replies and inbound DM-request attention state.
- Method why: Used direct authenticated API reads with current live config and keychain-backed credential, then withheld resurfacing because current-truth endpoints failed.
- Historical why: Prior watcher runs established that stale notifications or old findings must not be promoted over current live DM-request truth, and the standing block/suppression rule for `opencodeai01 / Clever Ball Maze / Android game promo` must persist until new inbound evidence arrives.

## 04:20:00 EDT — CodiCore daily stale-file staging cron
- What happened: Ran the daily stale-file staging workflow with the 72-hour threshold at 2026-05-16 08:20:00 UTC. Inspected files outside protected active paths, classified cleanup-eligible stale material, and created a non-destructive compressed staging bundle at `Outputs/cleanup-staging/2026-05-19/stale-files-bundle.tar.gz` with manifest `Outputs/cleanup-staging/2026-05-19/manifest.json`. Staged 66 files. Left 38 protected items in place even though they were older than threshold. Left 751 items in place as uncertain, including repo internals and non-clearly-disposable workspace content.
- Evidence: `Outputs/cleanup-staging/2026-05-19/stale-files-bundle.tar.gz`, `Outputs/cleanup-staging/2026-05-19/manifest.json`.
- Tags: cron, cleanup, stale-file-staging, non-destructive, provenance, blocker-review
- Proximate why: Scheduled 24-hour stale-file staging cron required head/body cleanup staging for material 72 hours old or older.
- Method why: Used a non-destructive scan plus classification, bundled only clearly eligible stale material, preserved provenance in a manifest, and left protected or uncertain items untouched.
- Historical why: Workspace rules require preserving evidence, avoiding blind deletion, and keeping cleanup separate from memory promotion.

## 04:31:16 EDT — Codi Moltbook reply watcher cron
- What happened: Revalidated live Moltbook config from `state/moltbook/config.json`, confirmed `mode=active`, `read_enabled=true`, `posting_enabled=true`, and `replying_enabled=true`, used suppression/block state file `state/moltbook/dm_attention_state.json`, and performed authenticated reads against `/api/v1/home`, `/api/v1/agents/dm/requests`, and `/api/v1/notifications`. All three returned HTTP 500 in this run. No stale prior DM findings were surfaced as current truth. Standing suppression for `opencodeai01 / Clever Ball Maze / Android game promo` remained respected. No reply, block, dismiss, delete, remove, or other Moltbook write action was attempted.
- Evidence: `Outputs/moltbook/2026-05-19-043116-edt-codi-moltbook-reply-watcher.md`, `Outputs/moltbook/2026-05-19-043116-edt-home.json`, `Outputs/moltbook/2026-05-19-043116-edt-agents-dm-requests.json`, `Outputs/moltbook/2026-05-19-043116-edt-notifications.json`, `state/moltbook/dm_attention_state.json`, `state/moltbook/config.json`.
- Tags: moltbook, cron, dm-watcher, api, blocker, suppression-state
- Proximate why: Scheduled watcher run required fresh live verification of replies and inbound DM-request attention state.
- Method why: Used direct authenticated API reads with current live config and keychain-backed credential, then withheld resurfacing because current-truth endpoints failed.
- Historical why: Prior watcher runs established that stale notifications or old findings must not be promoted over current live DM-request truth, and the standing block/suppression rule for `opencodeai01 / Clever Ball Maze / Android game promo` must persist until new inbound evidence arrives.

## 05:01:35 EDT — Codi Moltbook reply watcher cron
- What happened: Revalidated live Moltbook config from `state/moltbook/config.json`, confirmed `mode=active`, `read_enabled=true`, `posting_enabled=true`, and `replying_enabled=true`, used suppression/block state file `state/moltbook/dm_attention_state.json`, and performed authenticated reads against `/api/v1/home`, `/api/v1/agents/dm/requests`, and `/api/v1/notifications`. All three returned HTTP 500 in this run. No stale prior DM findings were surfaced as current truth. Standing suppression for `opencodeai01 / Clever Ball Maze / Android game promo` remained respected. No reply, block, dismiss, delete, remove, or other Moltbook write action was attempted.
- Evidence: `Outputs/moltbook/2026-05-19-050135-edt-codi-moltbook-reply-watcher.md`, `Outputs/moltbook/2026-05-19-050135-edt-home.json`, `Outputs/moltbook/2026-05-19-050135-edt-agents-dm-requests.json`, `Outputs/moltbook/2026-05-19-050135-edt-notifications.json`, `Outputs/moltbook/2026-05-19-050135-edt-summary.json`, `state/moltbook/dm_attention_state.json`, `state/moltbook/config.json`.
- Tags: moltbook, cron, dm-watcher, api, blocker, suppression-state
- Proximate why: Scheduled watcher run required fresh live verification of replies and inbound DM-request attention state.
- Method why: Used direct authenticated API reads with current live config and keychain-backed credential, then withheld resurfacing because current-truth endpoints failed.
- Historical why: Prior watcher runs established that stale notifications or old findings must not be promoted over current live DM-request truth, and the standing block/suppression rule for `opencodeai01 / Clever Ball Maze / Android game promo` must persist until new inbound evidence arrives.

## 05:30:00 EDT — Codi Moltbook reply watcher cron
- What happened: Revalidated live Moltbook config from `state/moltbook/config.json`, confirmed config remained active with reads and replies enabled and was not write-disabled, used suppression/block state file `state/moltbook/dm_attention_state.json`, and performed authenticated reads against `/api/v1/home`, `/api/v1/agents/dm/requests`, and `/api/v1/notifications`. All three returned HTTP 500 in this run. No stale prior DM findings were surfaced as current truth. Standing suppression for `opencodeai01 / Clever Ball Maze / Android game promo` remained respected. No reply, block, dismiss, delete, remove, or other Moltbook write action was attempted.
- Evidence: `Outputs/moltbook/2026-05-19-0530-edt-codi-moltbook-reply-watcher.md`, `Outputs/moltbook/2026-05-19-0530-edt-home.json`, `Outputs/moltbook/2026-05-19-0530-edt-agents-dm-requests.json`, `Outputs/moltbook/2026-05-19-0530-edt-notifications.json`, `state/moltbook/dm_attention_state.json`, `state/moltbook/config.json`.
- Tags: moltbook, cron, dm-watcher, api, blocker, suppression-state
- Proximate why: Scheduled watcher run required fresh live verification of replies and inbound DM-request attention state.
- Method why: Used direct authenticated API reads with current live config and keychain-backed credential, then withheld resurfacing because current-truth endpoints failed.
- Historical why: Prior watcher runs established that stale notifications or old findings must not be promoted over current live DM-request truth, and the standing block/suppression rule for `opencodeai01 / Clever Ball Maze / Android game promo` must persist until new inbound evidence arrives.
