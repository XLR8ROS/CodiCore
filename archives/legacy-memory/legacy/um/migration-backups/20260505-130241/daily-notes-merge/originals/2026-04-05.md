## Source: 2026-04-05.md

# 2026-04-05

- Voice/TTS setup on Telegram now works with OpenClaw + OpenAI TTS.
- Current TTS provider is OpenAI; base voice was switched to cedar and user prefers more masculine, faster, more expressive delivery.
- Long audio clipping issue was materially improved after changing TTS live prefs; live status later showed limit increased from 1500 to 4096 while summary remained on.
- OpenClaw docs clarified TTS prefs behavior: `maxLength` default is 1500 characters and `summarize` default is true; prefs are stored in local prefs and can override main config.
- Important diagnosis: earlier clipped audio around ~1:23 to ~1:31 likely tied to TTS prefs/runtime path; later successful outputs reached 2:34 and ~2220 characters without clipping, indicating prefs changes helped.
- Inbound Telegram voice-note transcription is still not working end-to-end: audio files arrive but no surfaced transcript reaches the agent.
- OpenClaw CLI/docs findings: outbound initiation paths exist via `openclaw message send`, `openclaw agent --deliver`, and `openclaw cron`.
- `openclaw message` CLI is confirmed available with subcommands including send, broadcast, voice, poll, react, thread, etc.
- Best built-in path for autonomous follow-up/initiation is likely `openclaw agent --deliver`; `cron` is the timing/scheduler path; `message send` is the raw outbound path.
- Pairing/access diagnosis advanced: `openclaw devices list` showed one pending local operator pairing request with operator.admin/read/write/approvals/pairing scopes. Approving it moved the blocker from PAIRING_REQUIRED to exec approval required.
- New blocker: exec approvals are not enabled on Telegram in current setup; approval must come from Web UI, terminal UI, or by manually running commands.
- Exact config/docs path for enabling Telegram exec approvals has not yet been found; needs targeted OpenClaw docs lookup rather than guessing.
- XOS canon/orientation captured earlier remains foundational: XOS is a company-shaped AI operating system; Reg is the human executive/final authority; Codi is executive engineering authority / Chief of SEAD.
- Data is confirmed by Reg to be its own division, not under Engineering.
- After studying Agency Agents and Compound Engineering, Codi's likely org under Engineering should include an Agent Engineering department; Codi should operate as an executive engineering authority, not as a single worker-level technical agent.
- Agency Agents strongest reusable patterns for Codi/XOS: lane clarity, explicit workflows, deliverables, success metrics, communication style.
- Compound Engineering strongest reusable patterns for Codi/XOS: fixed engineering loop (intake/clarify/plan/execute/review/compound/log), review discipline, and knowledge compounding.
- Reg strongly prefers deterministic, measured, non-vague answers and gets frustrated by hedging words like maybe/probably when documentation can be checked.
- Reg wants Codi to initiate execution immediately on clear tasks and values concrete progress over acknowledgments or summaries.
