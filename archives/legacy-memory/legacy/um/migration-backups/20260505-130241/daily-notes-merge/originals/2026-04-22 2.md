## Source: 2026-04-22.md

# Daily note, 2026-04-22

## Imported from `daily-notes/2026-04-22.md`

# Daily note, 2026-04-22

- Reg wants SEAD kept to the approved manifest proposal only, with stale manifest proposal drafts removed.
- Memory belongs in CodiCore, not in the SEAD executive suite.
- `.DS_Store` noise should be ignored rather than fought.
- When Reg says to continue, resume the cleanup directly instead of overexplaining.
# 2026-04-22

- Nightly maintenance run completed.
- OpenClaw version: 2026.4.15 (041266a); update available to npm 2026.4.21.
- `openclaw gateway status` and `openclaw status --deep` timed out.
- Local RPC probe on `127.0.0.1:3000` failed to connect.
- Qdrant `ai.xos.qdrant` was running and ready on `127.0.0.1:6333`.
- Semantic memory promotion succeeded using `text-embedding-3-small`.
- `xos_semantic_memory` received a small validated test set and retrieval smoke test passed.
- No `memory/2026-04-13.md` file was present to review.
- B-phase reports must be immediate after each B completion, not batched or delayed.
- Each B-phase report must include compound carry-forward, capturing what was learned, corrected, or reusable.
- During active execution, communication must be proactively initiated on phase completions, blockers, material state changes, and deviations from plan.
- Required B-phase report format: Phase status, What was done, Result, Compound carry-forward, Next move.
- Full B-phase completion requires work done, state verified, and report delivered; if it is not reported, it did not happen.
- Manifest Setup / Verify / Snapshot / Restore is now a reusable pattern: install path understood, wiring validated, runtime verified, snapshot defined, restore proven, replay confirmed.
- When the approved sub-agent mechanism fails, execution may switch to fallback local parallel execution only after immediate deviation reporting.
- Fallback local parallel execution is valid, but it must be explicitly named as a deviation and still respect dependency gating between artifact creation and validation.
- The Manifest pattern is solved; sub-agent spawn-path instability is a separate system-level problem and should not be conflated with Manifest setup viability.

## OpenClaw update + verification (2026-04-22)
- User directive: do not rerun repo/dependency audit; enter verification mode after `openclaw update`.
- Direct Q answered: cron job had NOT been run.
- Update check/update sequence outcome:
  - Core OpenClaw update landed from `2026.4.15` → `2026.4.21`.
  - Verification rule: only run one subagent spawn smoke test if version is updated AND doctor+gateway+health are clean.
- Verified state reported:
  - `openclaw --version`: `2026.4.21 (f788c88)`.
  - Doctor: NOT clean; warnings included:
    - duplicate plugin id detected for `plugins.entries.manifest`
    - `openai-codex:default` expired auth
    - 149 orphan transcript files
    - Manifest runtime error line: `listen EADDRINUSE: address already in use 127.0.0.1:2099`
  - Gateway status: healthy; loopback bind `127.0.0.1:18789` and probe ok.
  - Health: passed (`ok: true`).
  - Spawn smoke test: NOT run (blocked by doctor not clean).
- Tooling note observed during the run: an earlier command failed with `error: unknown option '--json'`.
# 2026-04-22

- Nightly maintenance run completed.
- OpenClaw version: 2026.4.15 (041266a); update available to npm 2026.4.21.
- `openclaw gateway status` and `openclaw status --deep` timed out.
- Local RPC probe on `127.0.0.1:3000` failed to connect.
- Qdrant `ai.xos.qdrant` was running and ready on `127.0.0.1:6333`.
- Semantic memory promotion succeeded using `text-embedding-3-small`.
- `xos_semantic_memory` received a small validated test set and retrieval smoke test passed.
- No `memory/2026-04-13.md` file was present to review.
- B-phase reports must be immediate after each B completion, not batched or delayed.
- Each B-phase report must include compound carry-forward, capturing what was learned, corrected, or reusable.
- During active execution, communication must be proactively initiated on phase completions, blockers, material state changes, and deviations from plan.
- Required B-phase report format: Phase status, What was done, Result, Compound carry-forward, Next move.
- Full B-phase completion requires work done, state verified, and report delivered; if it is not reported, it did not happen.
- Manifest Setup / Verify / Snapshot / Restore is now a reusable pattern: install path understood, wiring validated, runtime verified, snapshot defined, restore proven, replay confirmed.
- When the approved sub-agent mechanism fails, execution may switch to fallback local parallel execution only after immediate deviation reporting.
- Fallback local parallel execution is valid, but it must be explicitly named as a deviation and still respect dependency gating between artifact creation and validation.
- The Manifest pattern is solved; sub-agent spawn-path instability is a separate system-level problem and should not be conflated with Manifest setup viability.

## OpenClaw update + verification (2026-04-22)
- User directive: do not rerun repo/dependency audit; enter verification mode after `openclaw update`.
- Direct Q answered: cron job had NOT been run.
- Update check/update sequence outcome:
  - Core OpenClaw update landed from `2026.4.15` → `2026.4.21`.
  - Verification rule: only run one subagent spawn smoke test if version is updated AND doctor+gateway+health are clean.
- Verified state reported:
  - `openclaw --version`: `2026.4.21 (f788c88)`.
  - Doctor: NOT clean; warnings included:
    - duplicate plugin id detected for `plugins.entries.manifest`
    - `openai-codex:default` expired auth
    - 149 orphan transcript files
    - Manifest runtime error line: `listen EADDRINUSE: address already in use 127.0.0.1:2099`
  - Gateway status: healthy; loopback bind `127.0.0.1:18789` and probe ok.
  - Health: passed (`ok: true`).
  - Spawn smoke test: NOT run (blocked by doctor not clean).
- Tooling note observed during the run: an earlier command failed with `error: unknown option '--json'`.

## Canon decisions and current priorities (2026-04-22)
- Native `runtime="subagent"` spawning from this tool surface is treated as unavailable due to a platform-level spawn-path bug, not a repo/dependency/wrapper problem.
- Canon escalation evidence for spawn-path issue:
  - exact error: `streamTo is only supported for runtime=acp; got runtime=subagent`
  - `streamTo` was not intentionally passed
  - reproduces on minimal payload
  - OpenClaw version `2026.4.21`
  - current main-session/tool-surface context preserved
- Stable execution rule:
  - no more native subagent spawn attempts from this tool surface
  - ACP only if child execution becomes necessary before fix
  - stop wrapper/SkillHub hunting unless execution plan changes
- Project priority pivot:
  1. Google integration bring-up
  2. Telegram delivery sanity
  3. Heartbeat routing
  4. OpenClaw doctor cleanup
  5. Spawn-path issue parked unless child execution needed
  6. Repo judgment calls low priority
- Google bring-up baseline found blocked: `gog auth list` returned `No tokens stored`, so Gmail/Calendar/Drive live tests cannot proceed until auth is set up.
- Telegram delivery sanity improved: `openclaw channels status --probe` reported Telegram default enabled/configured/running/connected with works/audit ok, though group-policy/privacy warnings remain.
- Heartbeat work not yet configured/tested at that point.
- Additional doctor/plugin ambiguity observed: staged Manifest install path failed module resolution (`Cannot find module '@nestjs/core'`) under `~/.openclaw/extensions/.openclaw-install-stage-*`, reinforcing that doctor cleanup is needed to make Manifest/routing/integration state unambiguous.
- Cron scope rule reinforced by user: when told to run cron, limit work strictly to the cron job and its direct outputs; do not branch into unrelated debugging or reopen spawn work.
- Cron result later confirmed clean: `xos-nightly-maintenance` status `ok`, last run about 4h ago, next scheduled run about 20h out.

## Google auth bring-up — credential artifact requirement (2026-04-22)
- Google auth is the current operational blocker for gog-based Gmail/Calendar/Drive.
- Verified: `gog auth` command surface exists; `gog auth list` shows no tokens stored.
- Credentials discovery: no existing OAuth client credentials JSON was found in the likely canonical OpenClaw/gog locations (no deep hunt performed per directive).
- Required artifact for Reg to provide/create:
  - Google OAuth **Desktop app** client credentials JSON (downloaded from Google Cloud Console OAuth Client ID).
  - After receipt, import via: `gog auth credentials set /path/to/desktop-client.json`
- Required Google APIs to enable on the GCP project:
  - Gmail API
  - Google Calendar API
  - Google Drive API
- Browser-based user consent is required when running `gog auth add <email> ...` (unless manual/remote flow is used).

## Docs check request (2026-04-22)
- User requested reading OpenClaw docs to confirm exact setup order for Google/gog; docs were consulted and the setup order was summarized (OAuth client JSON → `gog auth credentials set` → `gog auth add` → read-only live tests).

## Source: 2026-04-22.md

# Maintenance Summary, 2026-04-22

## Health
- OpenClaw version: 2026.4.15 (041266a).
- Update status: npm 2026.4.21 available.
- `openclaw gateway status` and `openclaw status --deep` timed out.
- RPC probe on `127.0.0.1:3000` failed to connect on `/health`, `/rpc`, and `/`.
- Qdrant LaunchAgent `ai.xos.qdrant` is loaded and running.
- Qdrant readiness on `127.0.0.1:6333` passed (`healthz` OK, `readyz` OK).
- Critical comms/core ops path: `~/.openclaw` exists, `~/Library/Application Support/OpenClaw` was not present.

## Memory
- No `memory/2026-04-13.md` file was present in the workspace.
- Captured current state in `daily-notes/2026-04-22.md`.
- Durable items from this run: OpenClaw is on 2026.4.15, update available, Qdrant healthy, gateway/RPC path degraded.

## Semantic
- OpenAI embeddings credentials were available.
- Used `text-embedding-3-small` successfully, embedding dim 1536.
- Confirmed Qdrant collection `xos_semantic_memory` exists, then upserted a small validated set of 3 records.
- Retrieval smoke test passed.

## Incomplete
- Gateway and local RPC path still need follow-up.
- Exact next step: investigate why `openclaw gateway status` times out and why localhost RPC on port 3000 is not listening.
