# Daily Notes for 2026-05-04

## Summary of QMD Documentation

- QMD is a mini CLI search engine focused on local document, notes, and knowledge base search.
- It supports keyword, semantic, and hybrid search with reranking.
- Combines BM25 full-text search, vector semantic search, and LLM re-ranking locally with node-llama-cpp and GGUF models.
- Supports collections of notes, docs, and meeting transcripts, organized by context metadata for better LLM context selection.
- Embedding generation and hybrid search commands available.
- Supports retrieval of documents by path, docid, or glob pattern.
- Output formats and integration with agents via JSON, files, and Model Context Protocol (MCP) server.
- MCP server can run over stdio or HTTP for persistent LLM model loading.
- HTTP MCP server supports endpoints for streaming queries and health checks.
- Integration with Claude Desktop and Claude Code via plugin or MCP config.
- Usable as a library in Node.js or Bun applications.

## Actions

- No immediate operational changes needed.
- Retain knowledge of QMD as a modern local search and agent tooling option.
